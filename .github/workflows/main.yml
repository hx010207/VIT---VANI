name: Legacy APK Workflow (Disabled)

on:
  workflow_dispatch:

# Note: The active CI/CD workflow for VaniGuard APK release builds is located at .github/workflows/release_apk.yml.
jobs:
  deprecated-stub:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is superseded by .github/workflows/release_apk.yml."
